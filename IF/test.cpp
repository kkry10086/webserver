#include<iostream>
#include<string>
#include"IF.h"

using std::cout;using std::cin;using std::endl;

int main(int argc,char** argv)
{
    IF *interface = new IF;


    delete(interface);

    return 0;
}
